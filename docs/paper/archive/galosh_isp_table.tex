% ISP compute-weight + streamability table (from isp_throughput_model.md)
\begin{table}[t]
\centering
\caption{Per-pixel compute and streaming-ISP suitability. GALOSH MAC/pixel is
\emph{measured} by instrumentation (\texttt{-DGALOSH\_OPCOUNT}); the others from
their standard algorithm parameters / Conv2d hooks. ``Stream'' = fits a
fixed-latency, line-buffered ISP stage (data-independent + regular local access).
GALOSH is the lightest \emph{and} the only light method that is genuinely
streamable, while also being best-perceptual among the blind methods.}
\label{tab:isp}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l r r c}
\toprule
Method & MAC/px & vs ours & Stream? \\
\midrule
\textbf{GALOSH (ours)} & \textbf{3{,}380} & \textbf{1.0$\times$} & \textbf{yes (demonstrated)} \\
NLM-CFA            & 13{,}225 & 3.9$\times$ & possible, 4$\times$ compute \\
BM3D-CFA           & $\sim$21{,}632 & 6.4$\times$ & \textbf{no} (content-dependent) \\
Blind2Unblind      & $\sim$72k\,/\,1.22M & 21\,/\,360$\times$ & no (U-Net buffers) \\
NAFNet (raw)$^{*}$ & $\sim$61k\,/\,242k & 18\,/\,71$\times$ & no (U-Net buffers) \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize Blind2Unblind: one forward / its 17-forward re-visible inference
(wf48-d5 U-Net). $^{*}$NAFNet has no public raw model --- shown as the MAC budget
of a SOTA restoration CNN \emph{if} applied to raw (4-ch packed), not a measured
result. BM3D's collaborative block matching is decided from content at run time,
so it is not a fixed-latency datapath at all. An ISP MAC array of
$0.5$--$1$~TMAC/s runs GALOSH real-time at 1080p--4K by the op-count model;
on-chip state is $\sim$200\,KB of INT16 line buffers (no frame buffer).}
\end{table}
